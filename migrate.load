LOAD DATABASE
     FROM mysql://root:kavyasql%402403@localhost:3306/attendance_tracker
     INTO postgresql://postgres:kavyapost%402403@localhost:5432/attendance_manager

 WITH data only, reset sequences, foreign keys,
      workers = 8,
      concurrency = 4,
      batch size = 100000

  SET client_encoding to 'utf8',
      maintenance_work_mem to '256MB',
      work_mem to '128MB';
